name: CI
on:
  workflow_dispatch:
    inputs:
      stage:
        description: 'Target stage:'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - prod

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: nc -zv -w 1 google.com 443
